#include <iostream>
#include <map>
#include <string>

template <typename <typename, typename> Box, typename Key, typename Value> Box<Value, Key> reverseMap(Box<Key, Value> const& data)
{
}

int main()
{
    std::map<int, std::string> box;
    auto box2 = reverseMap(box);

    return 0;
}
